name: TrustMCP
description: Scan a checked-out repository with the TrustMCP CLI.

outputs:
  finding-count:
    description: Total findings reported by TrustMCP.
    value: ${{ steps.scan.outputs.finding-count }}
  low-count:
    description: Count of low-severity findings.
    value: ${{ steps.scan.outputs.low-count }}
  medium-count:
    description: Count of medium-severity findings.
    value: ${{ steps.scan.outputs.medium-count }}
  high-count:
    description: Count of high-severity findings.
    value: ${{ steps.scan.outputs.high-count }}

inputs:
  target:
    description: Local path, gh:owner/repo shorthand, or public GitHub repository URL to scan. Defaults to the checked-out workspace.
    required: false
    default: ""
  format:
    description: Report output format.
    required: false
    default: json
  fail-on:
    description: Exit non-zero when a finding at or above this severity exists.
    required: false
    default: ""
  output-file:
    description: Optional file path for writing the selected rendered report.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Resolve action inputs
      id: resolve
      shell: bash
      env:
        INPUT_TARGET: ${{ inputs.target }}
        INPUT_FORMAT: ${{ inputs.format }}
        INPUT_FAIL_ON: ${{ inputs.fail-on }}
        INPUT_OUTPUT_FILE: ${{ inputs.output-file }}
      run: |
        set -euo pipefail
        target="$INPUT_TARGET"
        output_file="$INPUT_OUTPUT_FILE"

        if [ -z "$target" ]; then
          target="$GITHUB_WORKSPACE"
        fi

        if [ -n "$output_file" ]; then
          case "$output_file" in
            /*) ;;
            *) output_file="$GITHUB_WORKSPACE/$output_file" ;;
          esac

          output_dir=$(dirname "$output_file")
          if [ ! -d "$output_dir" ]; then
            printf '%s\n' "TrustMCP action error: output-file directory does not exist: $output_dir" >&2
            exit 1
          fi
        fi

        case "$INPUT_FORMAT" in
          text|json|markdown) ;;
          *)
            printf '%s\n' "TrustMCP action error: format must be 'text', 'json', or 'markdown'." >&2
            exit 1
            ;;
        esac

        if [ -n "$INPUT_FAIL_ON" ]; then
          case "$INPUT_FAIL_ON" in
            low|medium|high) ;;
            *)
              printf '%s\n' "TrustMCP action error: fail-on must be 'low', 'medium', or 'high'." >&2
              exit 1
              ;;
          esac
        fi

        case "$target" in
          gh:*|http://*|https://*) ;;
          *)
            if [ ! -e "$target" ]; then
              printf '%s\n' "TrustMCP action error: target does not exist: $target" >&2
              exit 1
            fi
            ;;
        esac

        printf 'target=%s\n' "$target" >> "$GITHUB_OUTPUT"
        printf 'output-file=%s\n' "$output_file" >> "$GITHUB_OUTPUT"

    - name: Install dependencies
      shell: bash
      working-directory: ${{ github.action_path }}
      run: npm ci

    - name: Build TrustMCP
      shell: bash
      working-directory: ${{ github.action_path }}
      run: npm run build

    - name: Run TrustMCP scan
      id: scan
      shell: bash
      working-directory: ${{ github.action_path }}
      env:
        TARGET: ${{ steps.resolve.outputs.target }}
        FORMAT: ${{ inputs.format }}
        FAIL_ON: ${{ inputs.fail-on }}
        OUTPUT_FILE: ${{ steps.resolve.outputs.output-file }}
      run: |
        set -euo pipefail
        node dist/action/main.js "$TARGET" "$FORMAT" "$FAIL_ON" "$OUTPUT_FILE"
