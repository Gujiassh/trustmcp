name: TrustMCP gate

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  trustmcp:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository under test
        uses: actions/checkout@v4

      - name: Run TrustMCP gate
        uses: Gujiassh/trustmcp@main
        with:
          target: ${{ github.workspace }}
          format: json
          fail-on: high
